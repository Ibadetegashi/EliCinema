@model IEnumerable<Cinema>

@{
    ViewData["Title"] = "List of Cinemas";
}
<em><h3>Cinemas</h3></em>
<a asp-action="Create">
    <i class="bi bi-plus-circle"></i> Add New
</a>
<hr />
@*<div class="row">
        <div class="col-md-8 offset-md-2">

            <table id="myActorTable" class="table table-hover table-sm">
                <thead>
                    <tr class="text-center">
                        <th>@Html.DisplayNameFor(model => model.Logo)</th>
                        <th>@Html.DisplayNameFor(model => model.Name)</th>
                        <th>@Html.DisplayNameFor(model => model.Description)</th>

                        <th>Actions</th>

                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td >
                                <img class="rounded-circle" src="@item.Logo" alt="@item.Name" style="max-width: 100px" />
                            </td>
                            <td >
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td >
                                @Html.DisplayFor(modelItem => item.Description)
                            </td>

                            <td  style="max-width:fit-content">
                                <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i>Edit </a>|
                                <a class="btn btn-outline-info" asp-action="Details" asp-route-id="@item.Id"><i class="bi bi-eye"></i>Details </a>|
                                <a class="btn btn-danger text-white" asp-action="Delete" asp-route-id="@item.Id"><i class="bi bi-trash"></i>Delete </a>
                            </td>

                        </tr>
                    }
                </tbody>

            </table>
            <div style="position:fixed; right:25px; bottom:25px;" class="text-white">
                <a asp-controller="Cinemas" asp-action="Create" class="btn badge-success">Add New</a>
            </div>

        </div>
    </div>*@
<table id="myTable" class="table table-hover  table-striped table-sm " >
    <thead>
        <tr>
            <th>
                Id
            </th>
            <th>
                Image
            </th>
            <th>
                Name
            </th>
            <th data-orderable="false"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr id="id_@item.Id">
                <td>
                    @item.Id
                </td>
                <td>
                    <img src="@item.Logo" alt="@item.Name" style="max-width: 100px;height:100px;" />
                </td>
                <td>
                    @item.Name
                </td>
                <td style="max-width:fit-content">
                    <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i>Edit </a> |
                    <a class="btn btn-outline-info" asp-action="Details" asp-route-id="@item.Id"><i class="bi bi-eye"></i>Details </a> |
                    <a class="btn btn-danger text-white " asp-action="Delete" asp-route-id="@item.Id"><i class="bi bi-trash"></i>Delete </a>
                </td>

            </tr>
        }
    </tbody>
</table>


@*@await Html.PartialAsync("_CreateItem", "Cinemas")*@
<br />
<br />